[build-system]
requires = ["hatchling>=1.31,<2"]
build-backend = "hatchling.build"

[project]
name = "tom-agent"
version = "0.5.2"
description = "Open-source-first personal multimodal AI agent runtime"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.34,<1",
  "pydantic>=2.10,<3",
  "httpx>=0.28,<1",
  "python-dotenv>=1.0,<2",
  "cryptography>=44,<47",
]

[project.optional-dependencies]
browser = ["playwright>=1.50,<2"]
voice = [
  "numpy>=1.26,<3",
  "faster-whisper>=1.2,<2",
]
voice-indic = [
  "numpy>=1.26,<3",
  "torch>=2.5,<3",
  "transformers>=4.57.3,<6",
  "soundfile>=0.13,<1",
  "parler-tts @ git+https://github.com/huggingface/parler-tts.git",
]
voice-qwen = [
  "numpy>=1.26,<3",
  "qwen-tts @ git+https://github.com/dffdeeq/Qwen3-TTS-streaming.git",
  "torch>=2.5,<3",
  "soundfile>=0.13,<1",
]
voice-qwen-space = [
  "gradio-client>=1.13,<2",
]
memory = [
  "numpy>=1.26,<3",
  "sentence-transformers>=5,<6",
]
voice-neural = [
  "numpy>=1.26,<3",
  "faster-whisper>=1.2,<2",
  "silero-vad>=6,<7",
  "onnxruntime>=1.24,<2",
  "pipecat-ai>=0.0.100,<2",
  "soxr>=1,<2",
]
dev = ["pytest>=8,<9", "pytest-asyncio>=0.25,<1", "ruff>=0.9,<1"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["tom"]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
